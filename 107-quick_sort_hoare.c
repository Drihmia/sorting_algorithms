#include "sort.h"

/**
 * quick_sort_hoare - Sorts an array using the heap sort algorithm.
 *
 * @array: Array to be sorted.
 * @size: Size of the array.
 */
void quick_sort_hoare(int *array, size_t size)
{
	if (!array || size < 2)
		return;
}
